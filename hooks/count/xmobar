#!/bin/bash
PIPE="$CONFIG_DIR"/xmobar

SCRIPTPATH=$(dirname "$0")
SCRIPTPATH=$( (cd "$SCRIPTPATH" && pwd) )

message=$(bash "$SCRIPTPATH"/progressbar "$1" "$2" "$3" | tr '\r' ' ')
echo "$message" >> "$PIPE"
